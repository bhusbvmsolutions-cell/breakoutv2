<%- contentFor('title') %> Add Venue
<%- contentFor('head') %>
<link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-lite.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
<link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet" />
<style>
  body { background: #f5f7fb; }
  .form-container { max-width: 100%; margin: 0 auto; }
  .card-custom { border-radius: 1rem; overflow: hidden; border: none; box-shadow: 0 0.5rem 1.5rem rgba(0,0,0,0.08); }
  .card-header-custom { background: linear-gradient(135deg, #fcad00 0%, #ffd966 100%); padding: 0.75rem 1.25rem; }
  .card-header-custom h4 { margin: 0; color: #2c3e50; font-weight: 600; font-size: 1.1rem; }
  .card-header-custom p { margin: 0; font-size: 0.8rem; color: #2c3e50; opacity: 0.9; }
  .remove-item {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
    background: linear-gradient(135deg, #ff4d4d, #d90429);
    color: #fff;
    border-radius: 10%;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 10px rgba(217, 4, 41, 0.3);
    float: right;
    margin-bottom: 3px;
  }

  .remove-item i {
    font-size: 16px;
  }

  .remove-item:hover {
    transform: scale(1.15) rotate(-8deg);
    box-shadow: 0 6px 16px rgba(217, 4, 41, 0.5);
    background: linear-gradient(135deg, #ff1f1f, #b00020);
  }

  .remove-item:active {
    transform: scale(0.95);
  }
</style>

<%- contentFor('body') %>
<div class="container py-4">
  <div class="form-container">
    <div class="card card-custom">
      <div class="card-header-custom">
        <h4><i class="bx bx-building me-2"></i> Add Venue</h4>
        <p class="mb-0">Create a new venue</p>
      </div>
      <div class="card-body p-4">
        <form action="/admin/venues/store" method="POST" enctype="multipart/form-data">
          <%- include('_form', { venue: null, locations, categories, experienceTypes, lookingFor, partyTypes, suitableTimes, budgetRanges }); %>
        </form>
      </div>
    </div>
  </div>
</div>

<%- contentFor('js') %>
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-lite.min.js"></script>